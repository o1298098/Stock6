<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Stock6.Views.StockUpPhoto">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="发送" x:Name="SendBtn"></ToolbarItem>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <!--<StackLayout>
        <Button x:Name="sdsdsd" Text="zxc"></Button>
            <ListView x:Name="photolist" BackgroundColor="Beige" HasUnevenRows="True" CachingStrategy="RecycleElement" FlowDirection="LeftToRight">
            
        </ListView>-->
          
        <Grid>
            <ScrollView x:Name="scrollview">
                    <FlexLayout x:Name="flexLayout"
                        Wrap="Wrap"
                        JustifyContent="SpaceAround"
                        FlowDirection="LeftToRight"/>
                </ScrollView>

                <ActivityIndicator x:Name="activityIndicator"
                           IsRunning="True"
                           VerticalOptions="Center" />
            </Grid><!--</StackLayout>-->
    </ContentPage.Content>
</ContentPage>